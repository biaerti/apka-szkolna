# Skopiuj do .env.local i uzupelnij (Supabase -> Project Settings -> API).
# Bez tych zmiennych aplikacja dziala lokalnie (localStorage), bez logowania.
VITE_SUPABASE_URL=https://wfwvfvdodmmtlwjyuevi.supabase.co
VITE_SUPABASE_ANON_KEY=

# Klucza OpenRoutera (generowanie kartkowek) NIE wpisuj tutaj - wszystko, co
# apka czyta przez VITE_*, laduje w publicznym bundlu JS. Klucz idzie do
# Supabase: Edge Functions -> Secrets -> OPENROUTER_API_KEY
# (patrz supabase/functions/generuj-kartkowke/index.ts).
